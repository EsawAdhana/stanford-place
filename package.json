{
  "name": "stanfordplace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run build -w packages/shared && npm run dev -w apps/web",
    "build": "npm run build -w packages/shared && npm run build -w apps/web",
    "lint": "npm --workspaces --if-present run lint"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
